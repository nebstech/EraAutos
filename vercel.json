cat <<EOF > vercel.json
{
  "version": 2,
  "buildCommand": "pip install psycopg2-binary && python manage.py migrate && python manage.py collectstatic --noinput",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "wsgi.py"
    }
  ]
}
EOF
