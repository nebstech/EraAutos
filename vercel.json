{
  "version": 2,
  "builds": [
    {
      "src": "EraAutos/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "EraAutos/wsgi.py"
    }
  ],
  "buildCommand": "pip install psycopg2-binary && pip install -r requirements.txt && python manage.py migrate && python manage.py collectstatic --noinput"
}
